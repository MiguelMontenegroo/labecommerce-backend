export type TUsers = {
    id: string,
    name: string,
    email: string,
    password: string,
    createdAt: number
  }
  export type TProducts = {
    id: string,
    name: string,
    price: Number,
    description: string,
    imageUrl: string
  }